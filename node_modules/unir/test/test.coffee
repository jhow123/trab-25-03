cucina = require '../src/cucina'
expect = require('chai').expect
assert = require('chai').assert
should = require('chai').should()

describe('main', () ->
  it('should work with assert', () ->
    assert.equal(cucina.test(), 'works')
  )
)
